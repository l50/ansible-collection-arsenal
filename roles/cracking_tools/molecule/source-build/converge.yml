---
- name: Converge
  hosts: all
  gather_facts: true
  tasks:
    - name: Include default variables
      ansible.builtin.include_vars:
        file: "../../defaults/main.yml"

    - name: Include role under test
      ansible.builtin.include_role:
        name: l50.arsenal.cracking_tools
      vars:
        # Build hashcat from source with Rust support
        cracking_tools_hashcat_from_source: true
        # Disable GPU support in containers
        cracking_tools_gpu_support: false
        # Skip SecLists to speed up testing
        cracking_tools_install_seclists: false
        # Skip John to focus on hashcat source build
        cracking_tools_install_john: false
