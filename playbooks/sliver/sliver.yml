---
- name: Sliver
  hosts: all
  tasks:
    - name: Install Sliver c2
      ansible.builtin.include_role:
        name: l50.arsenal.sliver
      vars:
        asdf_plugins:
          - name: golang
            version: "1.23.5"
            scope: "global"
